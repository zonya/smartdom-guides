---
interface Props {
  href: string;
  title: string;
  description?: string;
  pubDate: Date;
  tags?: string[];
  readingTime?: number;
}

const { href, title, description, pubDate, tags = [], readingTime } = Astro.props;
---

<article class="card">
  <h3 class="card__title"><a href={href}>{title}</a></h3>
  <p class="card__meta">
    <time datetime={pubDate.toISOString()}>
      {pubDate.toLocaleDateString('sr-Latn-RS', { day: 'numeric', month: 'long', year: 'numeric' })}
    </time>
    {readingTime && <span class="card__dot">·</span>}
    {readingTime && <span>{readingTime} min čitanja</span>}
  </p>
  {description && <p class="card__desc">{description}</p>}
  {
    tags.length > 0 && (
      <p class="tags">
        {tags.map((tag) => (
          <a class="tag" href={`/tag/${tag}/`}>
            {tag}
          </a>
        ))}
      </p>
    )
  }
</article>
